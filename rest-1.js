// Crie uma função que some todos os parâmetros passados a ela.
const sumAll = () => {};

// Resultado esperado ao passar 1 e 2 como parâmetro para a função: 3
// ex: sumAll(1, 2) -> 3
// console.log(sumAll(1, 2));

// Resultado esperado ao passar 1, 2 e 3 como parâmetro para a função: 6
// ex: sumAll(1, 2, 3) -> 6
// console.log(sumAll(1, 2, 3));

// Resultado esperado ao passar 1, 2, 3, 4 e 5 como parâmetro para a função: 15
// ex: sumAll(1, 2, 3, 4, 5) -> 15
// console.log(sumAll(1, 2, 3, 4, 5));
